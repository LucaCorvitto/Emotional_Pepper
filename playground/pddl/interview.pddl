(define (problem interview)
    (:domain pepper_domain)

    (:objects
        int - interview
    )
	
	(:init
			(unknown (difficulty_medium))
			(unknown (difficulty_hard))    
    )
    
    (:goal (classification_done))
)
